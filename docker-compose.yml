version: "3.9"

services:
  app:
    build: .
    container_name: chat-quick-chat-server
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./data:/app/data
      - ./storage:/app/storage
    restart: unless-stopped
